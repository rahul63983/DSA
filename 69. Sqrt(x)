class Solution {
    public int mySqrt(int x) {
        int r=1;
        if (x==0)
            return x;
        for(long i=1;i<=x/2;i++)
        {
            if(i*i<=x)
                r=(int)i;
            else
                break;
            
        }

        return r;
        
    }
}
